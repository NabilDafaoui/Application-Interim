<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- TextView pour le nom -->
    <TextView
        android:id="@+id/textViewNom"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/guideline"
        android:text="Nom" />

    <!-- Guideline pour séparer Nom et Prénom -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.3" />

    <!-- TextView pour le prénom -->
    <TextView
        android:id="@+id/textViewPrenom"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@+id/textViewNom"
        app:layout_constraintStart_toStartOf="@+id/textViewNom"
        app:layout_constraintEnd_toStartOf="@+id/guideline"
        android:text="Prénom" />

    <!-- TextView pour l'email -->
    <TextView
        android:id="@+id/textViewEmail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/textViewPrenom"
        app:layout_constraintStart_toStartOf="@+id/textViewNom"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"
        android:text="Email" />

    <!-- Bouton pour accepter -->
    <Button
        android:id="@+id/buttonAccepter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Accepter"
        app:layout_constraintTop_toBottomOf="@+id/textViewEmail"
        app:layout_constraintEnd_toStartOf="@+id/buttonRefuser"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp" />

    <!-- Bouton pour refuser -->
    <Button
        android:id="@+id/buttonRefuser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Refuser"
        app:layout_constraintTop_toTopOf="@+id/buttonAccepter"
        app:layout_constraintEnd_toStartOf="@+id/buttonRepondre"
        android:layout_marginEnd="8dp" />

    <!-- Bouton pour répondre -->
    <Button
        android:id="@+id/buttonRepondre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Répondre"
        app:layout_constraintTop_toTopOf="@+id/buttonAccepter"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="8dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
